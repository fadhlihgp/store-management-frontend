import Layout from "../../widgets/Layout";
import {NoteContainer} from "../../features/Note";

export const Note = () => {
    return(
        <Layout pageTitle={"Catatan"} >
            <NoteContainer />
        </Layout>
    )
}
