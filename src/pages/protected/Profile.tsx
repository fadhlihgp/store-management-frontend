import Layout from "../../widgets/Layout";
import {ProfileFormContainer} from "../../features/Profile";

export const Profile = () => {
    return(
        <Layout pageTitle={"Profile"}>
            <ProfileFormContainer />
        </Layout>
    )
}
