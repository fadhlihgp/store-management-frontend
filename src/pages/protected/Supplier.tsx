import { SupplierContainer } from "../../features/supplier/SupplierList"
import Layout from "../../widgets/Layout"

export const Supplier = () => {
    return(
        <Layout pageTitle="Supplier">
            <SupplierContainer />
        </Layout>
    )
}